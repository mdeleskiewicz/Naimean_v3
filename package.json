{
  "name": "naimean-v3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('No build step')\"",
    "test": "node --test",
    "sync": "pwsh sync_naimean.ps1"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
